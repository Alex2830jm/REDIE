//const groupPermissions = document.getElementById("group-permissions");
const groupTemas = document.getElementById("group-temas");

//const permissionsSelected = document.getElementById("permissions-selected");
const temasSelected = document.getElementById("temas-selected");

/* new Sortable(groupPermissions, {
    group: 'shared',
    animation: 150
})

new Sortable(permissionsSelected, {
    group: 'shared',
    animation: 150
}) */


new Sortable(groupTemas, {
    group: 'shared',
    animation: 150
})

new Sortable(temasSelected, {
    group: 'shared',
    animation: 150
})